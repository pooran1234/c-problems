#include <stdio.h>
#include <string.h>

int main() {
    char str[50];
    int length;

    printf("Enter a string: ");
    scanf("%s", str);

    length = strlen(str);

    printf("Length = %d", length);

    return 0;
}
